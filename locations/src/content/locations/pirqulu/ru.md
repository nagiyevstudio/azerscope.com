## Черновик

Это пример записи-черновика, добавленный для проверки каркаса. Отредактируйте
содержимое через локальную форму (`npm run dev` →
`http://localhost:4321/locations/admin`) или прямо в этих файлах.

Черновики (`status: draft`) не попадают в каталог, на страницы и в JSON.
